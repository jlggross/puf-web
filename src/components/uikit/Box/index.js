import styled from 'styled-components'

import {
	background,
	padding,
	margin,
	flexbox,
	font,
} from '~/components/Theme/styled'

export const Box = styled('div')`
	${background}
	${padding}
	${margin}
  ${flexbox}
  ${font}
`
